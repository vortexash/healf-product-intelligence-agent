# Screenshots

- `app.png` - the chat interface (empty state with the sidebar, example prompt, and composer).
- `user-flow.png` - the user-journey infographic (steps, capabilities, and what happens behind the scenes).

The full text of real captured responses is in [`../example_outputs.md`](../example_outputs.md),
and the app is live at https://healf-product-intelligence-agent.vercel.app if you want to try any
scenario directly.
